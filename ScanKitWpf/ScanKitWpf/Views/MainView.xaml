<Window x:Class="ScanKitWpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:halcon="clr-namespace:HalconDotNet;assembly=halcondotnet" 
        xmlns:local="clr-namespace:ScanKitWpf.Views"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="" Height="1024" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <GroupBox Header="相机采集" FontSize="10" Style="{x:Null}">
                <halcon:HSmartWindowControlWPF x:Name="Hsmart"></halcon:HSmartWindowControlWPF>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="用户界面" FontSize="10" Style="{x:Null}" VerticalContentAlignment="Center">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <Button x:Name="OpenCamera" Content="打开相机" Style="{DynamicResource ButtonPrimary}"></Button>
                    <Button x:Name="OneGrap" Margin="0,20" Content="图像抓拍" Style="{DynamicResource ButtonPrimary}"/>
                    <Button x:Name="CloseCamera" Content="关闭相机" Style="{DynamicResource ButtonPrimary}"/>
                </StackPanel>
            </GroupBox>
        </Grid>
        <TextBox Grid.Row="1" x:Name="rtxtMsg" VerticalScrollBarVisibility="Auto" IsReadOnly="True" AcceptsReturn="True"  TextWrapping="WrapWithOverflow"
                 cal:Message.Attach="[Event TextChanged]=[Action ScrollToEnd($source)]"/>
    </Grid>
</Window>
