<Window x:Class="ScanKitWpf.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:halcon="clr-namespace:HalconDotNet;assembly=halcondotnet" 
        xmlns:local="clr-namespace:ScanKitWpf.Views"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="" Height="800" Width="1000">
    <hc:TransitioningContentControl>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"></RowDefinition>
                <RowDefinition Height="2*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <GroupBox Header="相机采集" FontSize="10" Style="{x:Null}">
                    <halcon:HSmartWindowControlWPF x:Name="Hsmart"></halcon:HSmartWindowControlWPF>
                </GroupBox>
                <hc:TabControl Grid.Column="1" ShowContextMenu="False" ShowOverflowButton="False" ShowScrollButton="True" IsScrollable="True" IsTabFillEnabled="True">
                    <hc:TabItem Header="相机运行" IsSelected="True">
                        <hc:SimpleStackPanel VerticalAlignment="Center" Background="LightSkyBlue">
                            <Button x:Name="OpenCamera" Content="打开相机" Style="{DynamicResource ButtonPrimary}"></Button>
                            <Button x:Name="OneGrab" Margin="0,20" Content="图像抓拍" Style="{DynamicResource ButtonSuccess}"/>
                            <Button x:Name="CloseCamera" Content="关闭相机" Style="{DynamicResource ButtonWarning}"/>
                        </hc:SimpleStackPanel>
                    </hc:TabItem>
                    <hc:TabItem Header="相机设置">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="程序名称：" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <TextBlock Text="相机ID：" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <TextBlock Text="曝光时间：" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <TextBox x:Name="DisplayName" Grid.Row="0" Grid.Column="1" Margin="2,2,0,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="Config_Camera_Name" Grid.Row="1" Grid.Column="1" Margin="2,2,0,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="Config_Camera_ExposeTime" Grid.Row="2" Grid.Column="1" Margin="2,2,0,0" VerticalAlignment="Center"/>
                            <Button Grid.ColumnSpan="2" Grid.Row="3" Content="保存设置" cal:Message.Attach="[Event Click]=[Action SaveConfig]" Style="{DynamicResource ButtonPrimary}" />
                        </Grid>
                    </hc:TabItem>
                    <hc:TabItem Header="条码设置">
                        <StackPanel Orientation="Vertical">
                            <GroupBox Header="条码选择" Style="{x:Null}">
                                <ListBox x:Name="Config_ScanCodeConfig">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox Content="{Binding CodeType}" IsChecked="{Binding IsChecked}"></CheckBox>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </GroupBox>
                            <GroupBox Header="解析返回" Style="{x:Null}">
                                <StackPanel Orientation="vertical" HorizontalAlignment="left">
                                <ListBox x:Name="Config_ParsingConfig">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <RadioButton GroupName="ReturnTypeGroup" Content="{Binding ReturnType}" IsChecked="{Binding IsChecked}"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                
                                    <!--<Expander>
                                        <Expander.Header>
                                            <RadioButton GroupName="RuleType" Content="分隔" Margin="2,2,0,0" HorizontalAlignment="Left"/>
                                        </Expander.Header>
                                    </Expander>
                                    <RadioButton GroupName="RuleType" Content="正则表达式" Margin="2,2,0,0" HorizontalAlignment="Left"/>-->

                                </StackPanel>
                            </GroupBox>
                            <Button x:Name="SaveConfig" Content="保存设置" Style="{DynamicResource ButtonPrimary}"></Button>
                        </StackPanel>
                    </hc:TabItem>
                    <hc:TabItem Header="通讯设置">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="IP：" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Center"></TextBlock>
                            <TextBox x:Name="Config_ServerIp" Grid.Column="1" Grid.Row="0" Margin="2,2,0,0" VerticalAlignment="Center"/>
                            <TextBlock Text="端口：" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center"></TextBlock>
                            <TextBox x:Name="Config_ServerPort" Grid.Column="1" Grid.Row="1" Margin="2,2,0,0" VerticalAlignment="Center"></TextBox>
                            <Button Grid.ColumnSpan="2" Grid.Row="2" Content="保存设置" cal:Message.Attach="[Event Click]=[Action SaveConfig]" Style="{DynamicResource ButtonPrimary}" />
                        </Grid>
                    </hc:TabItem>
                    <hc:TabItem Header="图片识别">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="10*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="图片路径:" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <TextBox x:Name="ImagePath" Grid.Column="1" Grid.Row="0" Margin="2,2,0,0" VerticalAlignment="Center"></TextBox>
                            <Button x:Name="ManualAnalyse" Content="手动识别" Grid.Row="1" Margin="0,10,0,0" Style="{DynamicResource ButtonPrimary}" Grid.ColumnSpan="2"/>
                        </Grid>
                    </hc:TabItem>
                </hc:TabControl>

            </Grid>
            <ListBox x:Name="lstMsg" Grid.Row="1" Margin="0,2,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="Wrap" Margin="0,-10,0,0" Text="{Binding}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <!--<TextBox Grid.Row="1" x:Name="rtxtMsg" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" AcceptsReturn="True"  TextWrapping="WrapWithOverflow"
                 cal:Message.Attach="[Event TextChanged]=[Action ScrollToEnd($source)]"/>-->
        </Grid>
    </hc:TransitioningContentControl>
</Window>
